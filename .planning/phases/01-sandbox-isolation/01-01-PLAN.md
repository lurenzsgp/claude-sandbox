---
phase: 01-sandbox-isolation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - esbuild.config.ts
  - Dockerfile
  - entrypoint.sh
  - src/cli.ts
autonomous: true
requirements:
  - CONT-02
  - CONT-04
  - AUTH-01

must_haves:
  truths:
    - "Running `npm run build` produces a single distributable CLI binary at dist/claude-sandbox.js"
    - "The Docker image can be built locally with `docker build --build-arg UID=$(id -u) --build-arg GID=$(id -g) -t claude-sandbox:latest .` and succeeds without error"
    - "Inside a container started from the image, `claude --version` succeeds (Claude Code is installed)"
    - "The image's entrypoint.sh reads /run/secrets/anthropic-api-key and exports ANTHROPIC_API_KEY — visible in `env` but not in `docker inspect` Env array"
    - "The container user inside the image matches the UID/GID passed as build args"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with bin field and all dependencies"
      contains: "\"bin\": { \"claude-sandbox\": \"dist/claude-sandbox.js\" }"
    - path: "Dockerfile"
      provides: "Container image definition with UID/GID build args and Claude Code"
      contains: "@anthropic-ai/claude-code"
    - path: "entrypoint.sh"
      provides: "Container startup script that injects the API key from secrets file"
      contains: "cat /run/secrets/anthropic-api-key"
    - path: "src/cli.ts"
      provides: "CLI entry point skeleton (shebang, Commander program setup)"
  key_links:
    - from: "package.json bin field"
      to: "dist/claude-sandbox.js"
      via: "esbuild bundle"
      pattern: "dist/claude-sandbox\\.js"
    - from: "Dockerfile ARG UID/GID"
      to: "useradd -u ${UID}"
      via: "RUN groupadd/useradd"
      pattern: "ARG UID"
    - from: "entrypoint.sh"
      to: "ANTHROPIC_API_KEY"
      via: "export ANTHROPIC_API_KEY=$(cat /run/secrets/...)"
      pattern: "export ANTHROPIC_API_KEY"
---

<objective>
Bootstrap the project: establish the TypeScript + esbuild build system, write the Dockerfile (with UID/GID matching and Claude Code pre-install), and create the container entrypoint script for API key injection.

Purpose: Every subsequent plan depends on a compiling project and a working Docker image. This is the foundation that must exist before any Docker operations can be coded or tested.

Output:
- package.json with all dependencies and a `bin` field
- tsconfig.json + esbuild build config
- Dockerfile that accepts UID/GID build args and installs @anthropic-ai/claude-code
- entrypoint.sh that injects ANTHROPIC_API_KEY from /run/secrets/ into the environment
- src/cli.ts skeleton (shebang + Commander program, no commands yet)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sandbox-isolation/01-CONTEXT.md
@.planning/phases/01-sandbox-isolation/01-RESEARCH.md

<interfaces>
<!-- This is a fresh project — no existing code. The interfaces below are the contracts this plan CREATES. -->
<!-- Later plans will import from src/cli.ts and extend Commander commands. -->

State file schema (to be created in Plan 02):
```json
{
  "version": "1",
  "containerId": "abc123...",
  "status": "running",
  "mounts": ["/abs/path/to/repo1"],
  "createdAt": "2026-04-09T10:00:00.000Z",
  "lastStartedAt": "2026-04-09T10:00:00.000Z"
}
```

Container name constant (to be used across all plans): `"claude-sandbox"`
Image tag constant: `"claude-sandbox:latest"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project scaffold (package.json, tsconfig, esbuild config)</name>
  <read_first>
    - /Users/lcazzoli/workspace/claude-tests/claude-sandbox/.planning/phases/01-sandbox-isolation/01-RESEARCH.md (standard stack versions and project structure)
  </read_first>
  <files>package.json, tsconfig.json, esbuild.config.ts, .gitignore</files>
  <action>
Create the following files exactly as specified:

**package.json** — use these exact values:
```json
{
  "name": "claude-sandbox",
  "version": "0.1.0",
  "description": "Run Claude Code in an isolated Docker sandbox with selective repo access",
  "type": "module",
  "bin": {
    "claude-sandbox": "dist/claude-sandbox.js"
  },
  "scripts": {
    "build": "tsx esbuild.config.ts",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "14.0.3",
    "dockerode": "4.0.10",
    "ignore": "7.0.5"
  },
  "devDependencies": {
    "@types/dockerode": "4.0.1",
    "@types/node": "25.5.2",
    "esbuild": "0.28.0",
    "tsx": "4.21.0",
    "typescript": "6.0.2",
    "vitest": "4.1.4"
  }
}
```

**tsconfig.json**:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*", "esbuild.config.ts"]
}
```

**esbuild.config.ts** — bundles the CLI to a single file with shebang:
```typescript
import { build } from 'esbuild';

await build({
  entryPoints: ['src/cli.ts'],
  bundle: true,
  platform: 'node',
  target: 'node18',
  format: 'esm',
  outfile: 'dist/claude-sandbox.js',
  banner: {
    js: '#!/usr/bin/env node',
  },
  external: [], // bundle everything including deps
});

console.log('Build complete: dist/claude-sandbox.js');
```

**.gitignore**:
```
node_modules/
dist/
~/.claude-sandbox/
*.js.map
```

Then run `npm install` to install all dependencies.
  </action>
  <verify>
    <automated>cd /Users/lcazzoli/workspace/claude-tests/claude-sandbox && node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(p.bin['claude-sandbox'], p.dependencies.commander, p.dependencies.dockerode)" && ls node_modules/commander node_modules/dockerode node_modules/ignore</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"claude-sandbox": "dist/claude-sandbox.js"` in the bin field
    - package.json contains `"commander": "14.0.3"` in dependencies
    - package.json contains `"dockerode": "4.0.10"` in dependencies
    - package.json contains `"ignore": "7.0.5"` in dependencies
    - node_modules/commander directory exists (npm install succeeded)
    - node_modules/dockerode directory exists
    - tsconfig.json contains `"moduleResolution": "bundler"`
    - esbuild.config.ts contains `outfile: 'dist/claude-sandbox.js'`
  </acceptance_criteria>
  <done>package.json, tsconfig.json, esbuild.config.ts, .gitignore written; `npm install` succeeds with zero errors; node_modules populated.</done>
</task>

<task type="auto">
  <name>Task 2: Create Dockerfile and entrypoint.sh (CONT-02, CONT-04, AUTH-01)</name>
  <read_first>
    - /Users/lcazzoli/workspace/claude-tests/claude-sandbox/.planning/phases/01-sandbox-isolation/01-RESEARCH.md (Pattern 2: Secrets File Injection; Pattern 3: UID/GID Matching; Pitfall 1: wrong package name; Pitfall 2: UID/GID mismatch; Pitfall 3: API key in docker inspect)
    - /Users/lcazzoli/workspace/claude-tests/claude-sandbox/.planning/phases/01-sandbox-isolation/01-CONTEXT.md (D-07, D-08, D-09 for image build decisions)
  </read_first>
  <files>Dockerfile, entrypoint.sh</files>
  <action>
Create **Dockerfile** in the project root:
```dockerfile
FROM node:22-bookworm

# UID/GID matching — must match host user (CONT-02)
# Passed as build args at image creation time via `docker build --build-arg`
ARG UID=1000
ARG GID=1000

# Install Claude Code CLI (CONT-04)
# IMPORTANT: Use @anthropic-ai/claude-code — NOT @anthropic-sdk/claude-code (wrong package)
RUN npm install -g @anthropic-ai/claude-code@latest

# Create sandbox user with matching UID/GID
RUN groupadd -f -g ${GID} sandbox && \
    useradd -m -u ${UID} -g ${GID} -s /bin/bash sandbox

# Copy and configure entrypoint for secrets injection (AUTH-01)
COPY entrypoint.sh /usr/local/bin/entrypoint.sh
RUN chmod +x /usr/local/bin/entrypoint.sh

# Switch to sandbox user
USER sandbox

# Default working directory is /workspace (all repos mounted here)
WORKDIR /workspace

ENTRYPOINT ["/usr/local/bin/entrypoint.sh"]
CMD ["/bin/bash"]
```

Create **entrypoint.sh** in the project root:
```bash
#!/bin/bash
set -e

# Inject API key from secrets file (AUTH-01)
# The secrets file is bind-mounted to /run/secrets/anthropic-api-key at container creation.
# Reading from the file prevents the key from appearing in `docker inspect` Env array.
if [ -f /run/secrets/anthropic-api-key ]; then
  export ANTHROPIC_API_KEY=$(cat /run/secrets/anthropic-api-key)
fi

# Set sandbox marker (D-13)
export CLAUDE_SANDBOX=1

exec "$@"
```

Make entrypoint.sh executable on the host as well:
```bash
chmod +x entrypoint.sh
```
  </action>
  <verify>
    <automated>cd /Users/lcazzoli/workspace/claude-tests/claude-sandbox && grep "ARG UID" Dockerfile && grep "@anthropic-ai/claude-code" Dockerfile && grep "useradd -m -u" Dockerfile && grep "cat /run/secrets/anthropic-api-key" entrypoint.sh && grep "export ANTHROPIC_API_KEY" entrypoint.sh && grep "CLAUDE_SANDBOX=1" entrypoint.sh</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile contains `ARG UID=1000` and `ARG GID=1000`
    - Dockerfile contains `@anthropic-ai/claude-code@latest` (not @anthropic-sdk)
    - Dockerfile contains `useradd -m -u ${UID} -g ${GID}`
    - Dockerfile contains `COPY entrypoint.sh /usr/local/bin/entrypoint.sh`
    - Dockerfile contains `USER sandbox` before `WORKDIR /workspace`
    - Dockerfile contains `ENTRYPOINT ["/usr/local/bin/entrypoint.sh"]`
    - entrypoint.sh contains `export ANTHROPIC_API_KEY=$(cat /run/secrets/anthropic-api-key)`
    - entrypoint.sh contains `export CLAUDE_SANDBOX=1`
    - entrypoint.sh contains `exec "$@"`
    - entrypoint.sh is executable (`ls -la entrypoint.sh` shows `-rwxr-xr-x`)
  </acceptance_criteria>
  <done>Dockerfile and entrypoint.sh written correctly. Dockerfile uses ARG UID/GID for host user matching, installs @anthropic-ai/claude-code, creates sandbox user. entrypoint.sh injects API key from secrets file and sets CLAUDE_SANDBOX=1.</done>
</task>

<task type="auto">
  <name>Task 3: Create CLI entry point skeleton (src/cli.ts) and verify build</name>
  <read_first>
    - /Users/lcazzoli/workspace/claude-tests/claude-sandbox/.planning/phases/01-sandbox-isolation/01-RESEARCH.md (architecture pattern — cli.ts is entry point, Commander.js subcommand structure)
    - /Users/lcazzoli/workspace/claude-tests/claude-sandbox/package.json (bin field, build script)
  </read_first>
  <files>src/cli.ts</files>
  <action>
Create directory `src/` and create `src/cli.ts`:
```typescript
#!/usr/bin/env node
import { Command } from 'commander';

const program = new Command();

program
  .name('claude-sandbox')
  .description('Run Claude Code in an isolated Docker sandbox with selective repo access')
  .version('0.1.0');

// Commands are registered in Plan 04 (lifecycle) and Plan 05 (shell)
// This skeleton is here so the build system can be verified independently

program.parse(process.argv);
```

Then run the build to verify the scaffold compiles:
```bash
npm run build
```

Verify the output file exists and has the shebang:
```bash
head -1 dist/claude-sandbox.js
```
Expected: `#!/usr/bin/env node`

Also verify the built file is executable and runs:
```bash
chmod +x dist/claude-sandbox.js
node dist/claude-sandbox.js --version
```
Expected output: `0.1.0`
  </action>
  <verify>
    <automated>cd /Users/lcazzoli/workspace/claude-tests/claude-sandbox && npm run build 2>&1 | grep -E "complete|error" && head -1 dist/claude-sandbox.js && node dist/claude-sandbox.js --version</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli.ts` exists and contains `import { Command } from 'commander'`
    - `src/cli.ts` contains `program.name('claude-sandbox')`
    - `npm run build` exits 0 (no TypeScript or esbuild errors)
    - `dist/claude-sandbox.js` first line is `#!/usr/bin/env node`
    - `node dist/claude-sandbox.js --version` outputs `0.1.0`
    - `dist/claude-sandbox.js` file size is >0 bytes
  </acceptance_criteria>
  <done>src/cli.ts created. `npm run build` produces dist/claude-sandbox.js with shebang. Running `node dist/claude-sandbox.js --version` prints 0.1.0.</done>
</task>

</tasks>

<verification>
After all tasks complete, run these checks:

1. Project structure: `ls package.json tsconfig.json esbuild.config.ts Dockerfile entrypoint.sh src/cli.ts dist/claude-sandbox.js`
2. Dependencies installed: `ls node_modules/commander node_modules/dockerode node_modules/ignore`
3. Build produces shebang: `head -1 dist/claude-sandbox.js` → `#!/usr/bin/env node`
4. CLI skeleton works: `node dist/claude-sandbox.js --version` → `0.1.0`
5. Dockerfile correctness: `grep -c "ARG UID\|@anthropic-ai/claude-code\|useradd" Dockerfile` → at least 3 matches
6. Entrypoint correctness: `grep -c "ANTHROPIC_API_KEY\|CLAUDE_SANDBOX" entrypoint.sh` → at least 2 matches

Note: Docker image build is NOT verified in this plan — that happens empirically when the secrets injector and lifecycle manager are wired in Plan 04. The Dockerfile and entrypoint.sh correctness is verified structurally here.
</verification>

<success_criteria>
- package.json, tsconfig.json, esbuild.config.ts, .gitignore exist and are correctly configured
- npm install succeeds with commander 14.0.3, dockerode 4.0.10, ignore 7.0.5 installed
- Dockerfile uses ARG UID/GID, installs @anthropic-ai/claude-code (not @anthropic-sdk), creates sandbox user, copies entrypoint.sh
- entrypoint.sh injects ANTHROPIC_API_KEY from /run/secrets/anthropic-api-key, sets CLAUDE_SANDBOX=1, calls exec "$@"
- src/cli.ts compiles via `npm run build` to dist/claude-sandbox.js with shebang
- `node dist/claude-sandbox.js --version` outputs `0.1.0`
</success_criteria>

<output>
After completion, create `.planning/phases/01-sandbox-isolation/01-01-SUMMARY.md` with:
- What was built (files created)
- Key decisions made during implementation (layer ordering, any deviations)
- Exact package versions installed (from npm install output)
- Any issues encountered and how they were resolved
- Interface contracts established for downstream plans
</output>
